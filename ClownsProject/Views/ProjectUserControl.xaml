<UserControl x:Class="Viewsss.ProjectUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Viewsss"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MouseDoubleClick="UserControl_MouseDoubleClick">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.RowSpan="2" Grid.ColumnSpan="2" Background="#FFF3F3F3" Margin="5" CornerRadius="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.5" />
            </Border.Effect>
        </Border>
        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Title}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Padding="10,5,10,0" />
        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Login}" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Padding="10,0,10,5" />
    </Grid>
</UserControl>
